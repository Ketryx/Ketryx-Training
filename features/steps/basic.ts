// Some basic Gherkin step definitions.
// These are only dummy implementations for demo purposes.

import { Given, Then, When } from '@cucumber/cucumber';

Given('iOS application is open',
  async function (this) {
  },
);

Given('Insulin dosage of {int} is entered',
  async function (this, value: number) {
  },
);

When('Form is submitted',
  async function (this) {
  },
);

Then('Insulin is not administered',
  async function (this) {
  },
);

Then('An error message is shown',
  async function (this) {
  },
);
